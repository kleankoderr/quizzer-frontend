name: Validate Frontend

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main
      - dev

jobs:
  validate:
    name: Validate Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install

      - name: Run Prettier auto-fix
        run: pnpm run format:write

      - name: Run ESLint
        run: pnpm run lint

      - name: Run tests
        run: pnpm run test:ci

      - name: Build project
        run: pnpm run build

      - name: Validation complete
        run: echo "✅ All validation checks passed! Vercel will now deploy automatically."
